apiVersion: v1
kind: Service
metadata:
  name: {{ include "young-certi.fullname" . }}
  labels:
    {{- include "young-certi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "young-certi.selectorLabels" . | nindent 4 }}
